[project]
name = "clinic-pipeline"
version = "0.1.0"
description = "Clinic pipeline for patient data scraping"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.0",
    "selenium>=4.33.0",
    "webdriver-manager>=4.0.2",
]
